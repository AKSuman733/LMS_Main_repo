{
  "name": "lmsproject",
  "private": true,
  "workspaces": [
    "lmsproject"
  ],
  "scripts": {
    "bootstrap": "npm install --workspaces",
    "dev": "npm --workspace lmsproject run dev",
    "build": "npm --workspace lmsproject run build",
    "preview": "npm --workspace lmsproject run preview"
  }
}
